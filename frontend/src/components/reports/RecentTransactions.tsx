import { useRecentTransactions } from "../../hooks/useReports";
export default function RecentTransactions() {
  const { data = [], isLoading, isError } = useRecentTransactions();
  return <div className="rounded-2xl border border-border bg-surface p-6 shadow-sm"><h2 className="mb-6 text-xl font-semibold text-text-primary">Recent Transactions</h2>{isError ? <p className="text-red-600">Unable to load transactions.</p> : isLoading ? <p className="text-text-secondary">Loading...</p> : data.length === 0 ? <p className="py-10 text-center text-text-secondary">No transactions yet.</p> : <div className="overflow-x-auto"><table className="w-full"><thead className="border-b border-border"><tr><th className="py-3 text-left text-text-secondary">Customer</th><th className="py-3 text-left text-text-secondary">Amount</th><th className="py-3 text-left text-text-secondary">Status</th></tr></thead><tbody>{data.map((t: any) => { const status = String(t.status || "Pending"); return <tr key={t.id} className="border-b border-border last:border-0"><td className="py-4 text-text-primary">{t.customer}</td><td>₹{Number(t.total).toLocaleString("en-IN")}</td><td><span className={`rounded-full px-3 py-1 text-xs font-medium ${status.toLowerCase() === "paid" ? "bg-green-100 text-green-700" : status.toLowerCase() === "cancelled" ? "bg-red-100 text-red-700" : "bg-yellow-100 text-yellow-700"}`}>{status}</span></td></tr>; })}</tbody></table></div>}</div>;
}

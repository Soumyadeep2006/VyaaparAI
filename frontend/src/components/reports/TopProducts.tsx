import { useProductAnalytics } from "../../hooks/useReports";
export default function TopProducts() {
  const { data = [], isLoading, isError } = useProductAnalytics();
  return <div className="rounded-2xl border border-border bg-surface p-6 shadow-sm"><h2 className="mb-6 text-xl font-semibold text-text-primary">Top Selling Products</h2>{isError ? <p className="text-red-600">Unable to load product analytics.</p> : isLoading ? <p className="text-text-secondary">Loading...</p> : data.length === 0 ? <p className="py-10 text-center text-text-secondary">No product sales yet.</p> : <div className="overflow-x-auto"><table className="w-full"><thead className="border-b border-border"><tr><th className="py-3 text-left text-text-secondary">Product</th><th className="py-3 text-left text-text-secondary">Sold</th><th className="py-3 text-left text-text-secondary">Revenue</th></tr></thead><tbody>{data.slice(0, 10).map((p: any) => <tr key={p.product} className="border-b border-border last:border-0"><td className="py-4 text-text-primary">{p.product}</td><td>{p.quantity}</td><td>₹{Number(p.sales).toLocaleString("en-IN")}</td></tr>)}</tbody></table></div>}</div>;
}
